{
  "$schema": "http://json-schema.org/draft-03/schema#",
  "title": "Tailnet Connection Helper configuration",
  "description": "Administrator configuration delivered through Chrome's managed storage. Set these under the extension's ID in the Admin console field 'Policy for extensions'. Values use the flat form, for example {\"companyName\": \"Acme\"}.",
  "type": "object",
  "properties": {
    "enabled": {
      "title": "Enabled",
      "description": "Set false to leave Chrome's normal error page in place without uninstalling the extension.",
      "type": "boolean"
    },
    "companyName": {
      "title": "Company name",
      "description": "Organisation name shown on the guidance page, for example 'Acme'. Leave unset for neutral wording that names no company.",
      "type": "string"
    },
    "watchedSuffixes": {
      "title": "Watched domain suffixes",
      "description": "Hostnames or suffixes this extension acts on, for example ['acme.ts.net']. A failed navigation to anything else is ignored and Chrome's own error page is left alone. Defaults to ['ts.net'], which matches every tailnet.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "tailnetName": {
      "title": "Tailnet display name",
      "description": "Name shown under the account row in the illustrated Tailscale menu, matching what users actually see in their client.",
      "type": "string"
    },
    "emailDomain": {
      "title": "Email domain",
      "description": "Domain used to render the example account in the illustration as you@example.com. Illustrative only, no address is read or sent.",
      "type": "string"
    },
    "supportUrl": {
      "title": "Support link",
      "description": "Where the escalation button sends a user who has already retried. Must be an https, mailto or slack URL. Leave unset to hide the button.",
      "type": "string"
    },
    "supportLabel": {
      "title": "Support button label",
      "description": "Text on the escalation button. Defaults to 'Ask IT'.",
      "type": "string"
    },
    "checkingLabel": {
      "title": "Checking label",
      "description": "Status shown for the moment between the page appearing and the connection check finishing. Default 'Checking your connection'.",
      "type": "string"
    },
    "showInstallLink": {
      "title": "Offer an install link",
      "description": "Whether to offer a Tailscale download link to a user who appears not to have the client running. Set false where Tailscale is deployed by MDM and users cannot or should not install it themselves, so the guidance points at your support route instead. Default true.",
      "type": "boolean"
    },
    "suggestCorrectTailnet": {
      "title": "Suggest the right tailnet",
      "description": "When a user fails to reach a host on a DIFFERENT tailnet, offer the equivalent address on yours. Off by default, because it is the one feature that acts on hosts outside watchedSuffixes. It only ever shows a suggestion, never the connection guidance, and the user can always continue to the address they typed.",
      "type": "boolean"
    },
    "tailscaleDownloadUrl": {
      "title": "Tailscale download link",
      "description": "Shown to users who have no Tailscale client at all. Defaults to the official download page.",
      "type": "string"
    },
    "openAppUrl": {
      "title": "Open Tailscale link",
      "description": "Link offered to a user who cannot find the Tailscale icon. Defaults to tailscale://connect, which launches the installed client after Chrome asks permission. It opens the app only, it does not connect. Accepts a tailscale: or https: URL, so a self-service portal can be used instead. Leave empty to hide the link.",
      "type": "string"
    },
    "openAppLabel": {
      "title": "Open Tailscale link label",
      "description": "Text of that link. Defaults to 'Open Tailscale'.",
      "type": "string"
    },
    "connectHelpUrl": {
      "title": "Internal runbook link",
      "description": "Optional link to your own connection instructions, shown alongside the built-in steps.",
      "type": "string"
    },
    "controlUrl": {
      "title": "Connectivity check URL",
      "description": "Plaintext http endpoint used to tell 'Tailscale is off' apart from 'this network needs a captive portal sign-in'. Must be http, not https: a portal cannot intercept https without a certificate the browser rejects, so an https probe can only ever report offline. Should answer 204 with no body. Fetched with no user data attached.",
      "type": "string"
    },
    "portalUrl": {
      "title": "Portal sign-in helper link",
      "description": "Plaintext http page offered as a button when a captive portal is detected. Opening any non-https page is what forces the network's sign-in screen to appear. Defaults to http://neverssl.com/, which exists for this purpose.",
      "type": "string"
    },
    "logoDataUrl": {
      "title": "Logo",
      "description": "Optional logo as a data:image/ URI. Remote URLs are rejected so the page never makes an outbound request.",
      "type": "string"
    },
    "bannerDataUrl": {
      "title": "Banner image",
      "description": "Optional banner shown across the top of the page, as a data:image/ URI. Remote URLs are rejected so the page never makes an outbound request, which matters because it is shown to users whose network is already broken. Maximum 1 MB.",
      "type": "string"
    },
    "accentColor": {
      "title": "Accent colour",
      "description": "Colour of the primary button and links, as a hex value such as #4a63d8. Only 3 or 6 digit hex is accepted. Pick something readable against a dark background, since the page is dark by design.",
      "type": "string"
    },
    "probeTimeoutMs": {
      "title": "Probe timeout (ms)",
      "description": "How long to wait for the local Tailscale check before calling it down. Default 1500.",
      "type": "integer"
    },
    "targetTimeoutMs": {
      "title": "App reachability timeout (ms)",
      "description": "How long to wait for the destination app before retrying. Default 4000.",
      "type": "integer"
    },
    "pollIntervalMs": {
      "title": "Poll interval (ms)",
      "description": "How often the guidance page rechecks connectivity. Default 2500.",
      "type": "integer"
    },
    "pollTimeoutMs": {
      "title": "Give up after (ms)",
      "description": "How long the guidance page keeps checking before it stops and says so. Default 120000, two minutes. Retrying forever keeps the service worker resident and tells the user success is imminent when it may not be.",
      "type": "integer"
    },
    "suppressMs": {
      "title": "Re-show suppression (ms)",
      "description": "How long to avoid re-showing the guidance page for the same tab and URL, so pressing Back does not bounce the user forward again. Default 8000.",
      "type": "integer"
    },
    "askItAfterAttempts": {
      "title": "Attempts before escalation",
      "description": "Number of failed visits to the same URL before the support button appears. Default 2.",
      "type": "integer"
    },
    "strings": {
      "title": "Copy overrides",
      "description": "Replaces the wording for any of the four states. Supports the placeholders {company}, {host}, {error}, {tailnetName} and {exampleEmail}. Override only the states you care about, the rest keep their defaults.",
      "type": "object",
      "properties": {
        "tailscaleOff": {
          "description": "Wording shown when Tailscale is not connected.",
          "type": "object",
          "properties": {
            "pill": {
              "description": "Short status label in the chip at the top of the page.",
              "type": "string"
            },
            "pillProbing": {
              "description": "Status label while the page is checking whether the app answers.",
              "type": "string"
            },
            "pillConnected": {
              "description": "Status label shown just before the page returns the user to the app.",
              "type": "string"
            },
            "pillGaveUp": {
              "description": "Status shown once the page has stopped checking.",
              "type": "string"
            },
            "headline": {
              "description": "Main heading. Placeholders are substituted as plain text.",
              "type": "string"
            },
            "lede": {
              "description": "Sentence under the heading explaining what failed.",
              "type": "string"
            },
            "steps": {
              "description": "Numbered instructions shown to the user.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        },
        "captivePortal": {
          "description": "Wording shown when a captive portal is intercepting traffic.",
          "type": "object",
          "properties": {
            "pill": {
              "description": "Short status label in the chip at the top of the page.",
              "type": "string"
            },
            "pillProbing": {
              "description": "Status label while the page is checking whether the app answers.",
              "type": "string"
            },
            "pillConnected": {
              "description": "Status label shown just before the page returns the user to the app.",
              "type": "string"
            },
            "pillGaveUp": {
              "description": "Status shown once the page has stopped checking.",
              "type": "string"
            },
            "headline": {
              "description": "Main heading. Placeholders are substituted as plain text.",
              "type": "string"
            },
            "lede": {
              "description": "Sentence under the heading explaining what failed.",
              "type": "string"
            },
            "steps": {
              "description": "Numbered instructions shown to the user.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        },
        "offline": {
          "description": "Wording shown when the device has no network at all.",
          "type": "object",
          "properties": {
            "pill": {
              "description": "Short status label in the chip at the top of the page.",
              "type": "string"
            },
            "pillProbing": {
              "description": "Status label while the page is checking whether the app answers.",
              "type": "string"
            },
            "pillConnected": {
              "description": "Status label shown just before the page returns the user to the app.",
              "type": "string"
            },
            "pillGaveUp": {
              "description": "Status shown once the page has stopped checking.",
              "type": "string"
            },
            "headline": {
              "description": "Main heading. Placeholders are substituted as plain text.",
              "type": "string"
            },
            "lede": {
              "description": "Sentence under the heading explaining what failed.",
              "type": "string"
            },
            "steps": {
              "description": "Numbered instructions shown to the user.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        },
        "appDown": {
          "description": "Wording shown when Tailscale is connected but the app did not answer.",
          "type": "object",
          "properties": {
            "pill": {
              "description": "Short status label in the chip at the top of the page.",
              "type": "string"
            },
            "pillProbing": {
              "description": "Status label while the page is checking whether the app answers.",
              "type": "string"
            },
            "pillConnected": {
              "description": "Status label shown just before the page returns the user to the app.",
              "type": "string"
            },
            "pillGaveUp": {
              "description": "Status shown once the page has stopped checking.",
              "type": "string"
            },
            "headline": {
              "description": "Main heading. Placeholders are substituted as plain text.",
              "type": "string"
            },
            "lede": {
              "description": "Sentence under the heading explaining what failed.",
              "type": "string"
            },
            "steps": {
              "description": "Numbered instructions shown to the user.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        },
        "nameNotFound": {
          "description": "Wording shown when Tailscale is connected but the name does not resolve.",
          "type": "object",
          "properties": {
            "pill": {
              "description": "Short status label in the chip at the top of the page.",
              "type": "string"
            },
            "pillProbing": {
              "description": "Status label while the page is checking whether the app answers.",
              "type": "string"
            },
            "pillConnected": {
              "description": "Status label shown just before the page returns the user to the app.",
              "type": "string"
            },
            "pillGaveUp": {
              "description": "Status shown once the page has stopped checking.",
              "type": "string"
            },
            "headline": {
              "description": "Main heading. Placeholders are substituted as plain text.",
              "type": "string"
            },
            "lede": {
              "description": "Sentence under the heading explaining what failed.",
              "type": "string"
            },
            "steps": {
              "description": "Numbered instructions shown to the user.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        },
        "wrongTailnet": {
          "description": "Wording shown when the address belongs to a different tailnet.",
          "type": "object",
          "properties": {
            "pill": {
              "description": "Short status label in the chip at the top of the page.",
              "type": "string"
            },
            "pillProbing": {
              "description": "Status label while the page is checking whether the app answers.",
              "type": "string"
            },
            "pillConnected": {
              "description": "Status label shown just before the page returns the user to the app.",
              "type": "string"
            },
            "pillGaveUp": {
              "description": "Status shown once the page has stopped checking.",
              "type": "string"
            },
            "headline": {
              "description": "Main heading. Placeholders are substituted as plain text.",
              "type": "string"
            },
            "lede": {
              "description": "Sentence under the heading explaining what failed.",
              "type": "string"
            },
            "steps": {
              "description": "Numbered instructions shown to the user.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      }
    }
  }
}
