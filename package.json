{
  "name": "tailnet-connection-helper",
  "version": "1.0.1",
  "private": true,
  "description": "Chrome extension that replaces the browser error page with connection guidance when a Tailscale tailnet app is unreachable.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "check": "node tools/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "node tools/check.mjs && node --test tests/*.test.mjs && python3 tools/validate_manifest.py",
    "preview": "python3 tools/preview_server.py",
    "package": "node tools/build.mjs --zip",
    "validate": "python3 tools/validate_manifest.py",
    "build": "node tools/build.mjs"
  }
}
